package com.example.eCommerceDemo.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class ProductController {

    /*
    Product → ProductController

    CRUD completo (listar productos, buscar por categoría, añadir, actualizar, borrar)

    Endpoint público para catálogo → lectura

    Endpoint administrativo → creación/edición
     */
}
